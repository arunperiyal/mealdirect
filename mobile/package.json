{
  "name": "mealdirect-mobile",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "customer",
    "restaurant",
    "admin",
    "delivery",
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
